{
  "buildCommand": "cd .. && pnpm install --frozen-lockfile && cd db && pnpm run build && cd ../api && pnpm run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "hono",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}